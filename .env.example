# Set the actual public origin before the production build/deployment.
# Absolute HTTP(S) origin only: no credentials, path, query or fragment.
# Leave empty locally to disable indexing and omit canonical/social image URLs.
SITE_URL=
